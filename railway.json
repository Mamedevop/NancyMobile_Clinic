{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix backend/src && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "deploy": {
    "startCommand": "node backend/src/server.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
